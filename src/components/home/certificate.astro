---
import certificate from "../../collections/certificate.json";
import CertificateData from "../certificateData.astro";
---

<section class="max-w-4xl mx-auto px-7 lg:px-0">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 dark:text-neutral-100"
  >
    Mis Certificados
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400">
    Estos certificados representan mi esfuerzo constante por aprender y mejorar
    en el desarrollo Back-End. Son una muestra de mi compromiso con la
    excelencia y mi deseo de seguir aportando valor en cada proyecto.
  </p>
  <div
    class="grid items-stretch w-full sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7"
  >
    {
      certificate.map((certificate) => {
        return (
          <CertificateData
            name={certificate.name}
            description={certificate.description}
            image={certificate.image}
            url={certificate.url}
          />
        );
      })
    }
  </div>
</section>
