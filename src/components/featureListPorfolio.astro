---
import featuresPorfolio from "../collections/featuresPorfolio.json";
---

<ul class="mt-2 list-disc list-inside text-neutral-600 dark:text-neutral-400">
    {featuresPorfolio.map((feature) => (
      <li style="margin-bottom: 10px;"> <!-- Ajusta el valor según tus necesidades -->
        <strong>{feature.title}:</strong> {feature.description}
      </li>
    ))}
  </ul>